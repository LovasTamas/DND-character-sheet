"""FastAPI HTTP/JSON layer for the D&D character-sheet engine.

See docs/webui-architecture.md for the API contract this package
implements. Nothing in `sheet_project.engine` is modified by this
package; it only calls the engine's public methods.
"""
